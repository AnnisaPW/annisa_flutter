part of '../_index.dart';

class HomeCoba extends StatelessWidget {
  const HomeCoba({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
      width: 300,
    );
  }
}
